{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend of our project",
  "main": "src/app.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx tsc && node dist/app.js",
    "frontend": "cd ../tp3-front && npm run start",
    "both": "concurrently \"npm run start\" \"npm run frontend\""
  },
  "author": "sylver, noemie, jason, mario",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.18",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "typescript": "^5.2.2"
  }
}
